## Backend environment example
## Copy to `backend/.env` and adjust as needed.

# PostgreSQL connection used by the FastAPI backend
# If you're using the provided docker-compose Postgres on the same machine:
DATABASE_URL=postgresql+asyncpg://alteus:password@localhost:5432/alteus_quizzer

# Optional: tighten/override CORS origin regex (defaults allow localhost/127.0.0.1/LAN IPs)
# CORS_ALLOW_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1|\d{1,3}(\.\d{1,3}){3})(:\d+)?$

# Alteus.ai LLM proxy (backend-only)
# NOTE: Use the correct names below. The code also supports the legacy typo env vars for backward compatibility:
# - ALTEUS_APY_KEY (typo) and ALTEUS_ENTPOINT_ID (typo)
ALTEUS_API_KEY=your-api-key-here
ALTEUS_API_URL=https://providers-api.tech.esolutions.ro/api/responses
ALTEUS_ENDPOINT_ID=2804ec65-c7ea-4410-9e40-b425a2c76341